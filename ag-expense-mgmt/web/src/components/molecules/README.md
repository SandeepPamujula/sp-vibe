# Molecules

Combinations of atoms that form simple, functional components.

## Examples
- Form fields (label + input + error message)
- Search bars (input + button)
- Card headers (title + action button)

## Guidelines
- Composed of 2 or more atoms
- Should serve a single purpose
- Should be reusable across different contexts
